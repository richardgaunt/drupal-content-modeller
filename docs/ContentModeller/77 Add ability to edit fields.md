

Properties we can change after field creation:
- Label
- Description
- Required

For Entity reference fields we can change:
```
target_bundles:  
  civictheme_image: civictheme_image  
  civictheme_remote_video: civictheme_remote_video
```

Please add a new project menu item for:

Edit field instance

We are then presented with entity types to select
Then we are presented with bundles.
Then we are presented with the fields on the bundles

Once we have selected a field we then can update the above.
